1. How would you choose between single-field, compound, and text indexes for the inventory search requirements?
2. How would you optimize the inventory API when search, category filtering, sorting, and pagination are used simultaneously?
3. How would you use $facet to generate multiple dashboard metrics in a single database operation?
4. How would you prevent race conditions that could allow two simultaneous requests to create the same SKU?
5. How would you prevent cache stampedes when many users request the same uncached data simultaneously?
6. How would you make the application completely environment-independent so it works on different machines without hardcoded localhost URLs?